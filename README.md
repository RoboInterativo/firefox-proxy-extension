# firefox-proxy-extension
Proxy only the sites you choose. Add domains to a list, set your proxy, and everything else stays direct. Supports HTTP, HTTPS, SOCKS4, SOCKS5, authentication, and subdomains.
